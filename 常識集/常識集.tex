\documentclass{ltjsarticle}

\setlength\parindent{0pt}
\setlength{\columnseprule}{0.4pt}

\newcommand{\sk}{\vskip\baselineskip}

\usepackage[margin=15mm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{mathrsfs}
\usepackage{bm}
\usepackage{here}
\usepackage{bbm}
\usepackage{graphicx}
\usepackage[unicode]{hyperref}
\usepackage{mathtools}
\usepackage{url}
\usepackage{ascmac}
\usepackage{physics}
\usepackage[version=4]{mhchem}
\usepackage{listings}

\lstset{
language=Python, 
basicstyle={\small},
identifierstyle={\small},
commentstyle={\small\itshape},
keywordstyle={\small\bfseries},
ndkeywordstyle={\small},
stringstyle={\small\ttfamily},
frame={tblr},
breaklines=true,
columns=[l]{fullflexible},
xrightmargin=0zw,
xleftmargin=3zw,
numberstyle={\scriptsize},
stepnumber=1,
numbersep=1zw,
lineskip=-0.5exx
}



\mathtoolsset{showonlyrefs=true}

\title{西森スピングラス 常識集}
\author{東京大学 理学系研究科物理学専攻 修士1年 平松大門}
\begin{document}
\maketitle
\section{相転移と平均場理論}
\subsection{Ising模型}
\begin{itemize}
    \item Ising模型のHamiltonianは？
    \[
    H = - J \sum_{(ij)\in B} S_i S_j - h\sum_i S_i
    \] 
\sk
    \item Boltzmann分布で状態$\bm{S}$となる確率は？
    \[
P(\bm{S})=\frac{\exp(-\beta H(\bm{S}))}{\mathop{\mathrm{Tr}}\limits_{\bm{S}}\, \exp(-\beta H(\bm{S}))}
    \]

    \sk
    \item 物理量$A$のカノニカル平均$\langle A \rangle$は？
    \[
    \langle A \rangle:=\frac{\mathop{\mathrm{Tr}}\limits_{\bm{S}}\, A(\bm{S})\,\exp(-\beta H(\bm{S}))}{\mathop{\mathrm{Tr}}\limits_{\bm{S}}\, \exp(-\beta H(\bm{S}))}
    \]
\end{itemize}

\newpage
\section{スピングラスの平均場理論}


\newpage
\section{レプリカ対称の破れ}

\newpage
\section{スピングラスのゲージ理論}

\newpage
\section{誤り訂正符号}



\newpage
\section{画像修復}
\subsection{確率論を用いた画像修復}
\begin{itemize}
    \item 画像修復が誤り訂正符号と似た話になるのはなぜか？
    
    画像ピクセルと符号ビットを同一視すれば当たり前。元画像の分布は符号の場合と異なり一様でないことに注意。
\end{itemize}

\subsubsection{劣化2値画像とBayes推定}
\begin{itemize}
    \item ピクセルのノイズ付き状態$\tau_i$が確率$p$で元の状態$\xi_i$から反転している時の出力分布は？
    \[
    P_{\mathrm{out}}(\tau_i | \xi_i)=\frac{\exp(\beta_p \tau_i\xi_i)}{2\cosh\beta_p}
    \]
ただし、$\exp(2\beta_p)=\frac{1-p}{p}$。
$\pm J$模型と同一である。

\sk
\item ノイズ付き画像$\{\tau_i\}$と原画像$\{\xi_i\}$に対して上の確率分布を拡張した式は？
\[
P_\mathrm{out}(\{\tau_i\}|\{\xi_i\})=\frac{\exp\left(\beta_p\displaystyle\sum_i \tau_i\xi_i\right)}{\left(2\cosh\beta_p \right)^N}
\]
最尤推定なら、これを最大にする$\{\xi_i\}$を修復画像として採用する。

\sk
\item ノイズ付き画像$\{\tau_i\}$から修復画像$\{\sigma_i\}$を生成する確率分布(事後分布)は？
\[
P(\{\sigma_i\}|\{\tau_i\})
=\frac{P_\mathrm{out}(\{\tau_i\}|\{\sigma_i\})P(\{\sigma_i\})}{\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}} P_\mathrm{out}(\{\tau_i\}|\{\sigma_i\})P(\{\sigma_i\})}
=\frac{\exp\left(\beta\displaystyle\sum_i \tau_i\sigma_i\right)P(\{\sigma_i\})}{\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}} \exp\left(\beta\displaystyle\sum_i \tau_i\sigma_i\right)P(\{\sigma_i\})}
\]

MAPなら、これを最大にする$\{\sigma_i\}$を修復画像として採用する。
MPMなら、$\mathrm{sgn}\langle \sigma_i\rangle :=\mathrm{sgn}\left(\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}}\sigma_i \,P(\{\sigma_i\}|\{\tau_i\})\right)$を修復ピクセルとして採用する。
ノイズパラメータ$\beta_p$は不明なので$\beta$とし、探す必要がある。

\sk
\item 事前分布$P(\{\sigma_i\})$のモデル分布$P_m(\{\sigma_i\})$は？
\[
P_m(\{\sigma_i\})=\frac{\exp\left(\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i \sigma_j\right)}{\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}}\exp\left(\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i\sigma_j\right)}
\]
自然な画像は連続的であることが多いため、隣接するピクセルが同じ状態である確率が高くなるようモデルを設定。
$\beta_m$は、大きくすると同じ状態を取りやすくなる、画像の滑らかさについてのパラメータ。

\end{itemize}

\subsubsection{MAPと有限温度復号}


\begin{itemize}
    \item 上のモデルを適用した事後分布は？
    \[
    P(\{\sigma_i\}|\{\tau_i\})
    =\frac{\exp\left(\beta\displaystyle\sum_i \tau_i\sigma_i+\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i \sigma_j\right)}{\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}} \exp\left(\beta\displaystyle\sum_i \tau_i\sigma_i+\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i \sigma_j\right)}
    \]
    $\frac{\beta_m}{\beta}$を相互作用と解釈すれば、これはランダムな磁場がかかったIsing模型のBoltzmann分布である。
\end{itemize}

\sk

\subsubsection{重なりのパラメータ}
\begin{itemize}
    \item 原画像$\{\xi_i\}$と修復画像$\{\sigma_i\}$のオーバーラップの期待値$M$は？
    
    \[
    M(\beta_m, \beta)
    := \mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\mathop{\mathrm{Tr}}\limits_{\{\tau_i\}} P_\mathrm{out}(\{\tau_i\}|\{\xi_i\})P(\{\xi_i\})\,\xi_i \,\mathrm{sgn}\langle \sigma_i\rangle
    \]
パラメータ$\beta_m, \beta$は$\langle \sigma_i\rangle$に含まれている。



    \sk
    \item オーバーラップ$M(\beta_m, \beta)$の満たす関係は？
    \[
    M(\beta_m, \beta)\leq M(\beta_s, \beta_p)
    \]
ただし、事前分布$P(\{\xi_i\})=\frac{\exp\left(\beta_s\displaystyle\sum_{\langle ij \rangle}\xi_i \xi_j\right)}{Z(\beta_s)}$を仮定した。
真のパラメータに合致する時に重なりが最大になるという、自然な関係。
証明は
\begin{align}
    M(\beta_m, \beta) 
&\leq \left|\mathop{\mathrm{Tr}}\limits_{\{\tau_i\}}\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}} P _\mathrm{out}(\{\tau_i\}|\{\xi_i\})P(\{\xi_i\})\,\xi_i \,\mathrm{sgn}\langle \sigma_i\rangle\right|\\
&\leq \mathop{\mathrm{Tr}}\limits_{\{\tau_i\}}\left|\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}} P(\{\xi_i\}|\{\tau_i\})P(\{\tau_i\})\,\xi_i\right|\\
&\leq \mathop{\mathrm{Tr}}\limits_{\{\tau_i\}}\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}} P(\{\xi_i\}|\{\tau_i\})P(\{\tau_i\})\,\xi_i \,\mathrm{sgn}\langle \xi_i\rangle = M(\beta_s, \beta_p) .\\
\end{align}

一般に、$x\,\mathrm{sgn}(y)\leq x\,\mathrm{sgn} (x)$の不等式が成り立つ。

\sk
\item ガウスノイズの場合の出力分布は？
\[
P_\mathrm{out}(\{\tau_i\}|\{\xi_i\})
=\frac{1}{\sqrt{2\pi\tau^2}^N}\exp\left[-\displaystyle\sum_i \frac{\left(\tau_i-\tau_0\xi_i\right)^2}{2\tau^2}\right]
\]

\sk
\item ガウスノイズの場合の事後分布は？
\[
P(\{\xi_i\}|\{\tau_i\})
=\frac{\exp\left(\frac{\tau_0}{\tau^2}\displaystyle\sum_i \tau_i\sigma_i+\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i \sigma_j\right)}{\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}}\exp\left(\frac{\tau_0}{\tau^2}\displaystyle\sum_i \tau_i\sigma_i+\beta_m\displaystyle\sum_{\langle ij \rangle}\sigma_i \sigma_j\right)}
\]


\sk
\item ガウスノイズの場合のオーバーラップ$M(\beta_m, \beta)$の満たす関係は？
\[
M(\beta_m, \beta)\leq M\left(\beta_s, \frac{\tau_0}{\tau^2}\right)
\]
証明は、$\mathop{\mathrm{Tr}}\limits_{\{\tau_i\}}$を$\displaystyle\int \prod_i d\tau_i$に置き換えるだけ。
\end{itemize}

\sk
\subsection{無限レンジ模型}
\subsubsection{レプリカ計算}
\begin{itemize}
    \item 無限レンジ模型での事前分布は？
    \[
    P(\{\xi_i\})= \frac{\exp\left(\frac{\beta_s}{2N}\displaystyle\sum_{i\neq j}\xi_i \xi_j\right)}{Z(\beta_s)}
    \]
    エネルギー部分が$N$にスケールするよう、相互作用に$\frac{1}{N}$を加える。
    モデル分布も同様。

    \sk
\item 画像修復における無限レンジ模型のハミルトニアンは？
\[
-\beta H := \frac{\beta_m}{2 N}\displaystyle\sum_{i\neq j}\sigma_i \sigma_j + \beta\displaystyle\sum_i \tau_i\sigma_i
\]
事後分布の最大化により画像を修復するが、今回はそれがBoltzmann分布であるから類推によりハミルトニアンを定義できる。
\sk
\item Gauss積分の公式は？
\[
\int dx\,\exp\left(-\alpha x^2+\beta x\right)=\exp\left(\frac{\beta^2}{4\alpha}\right)
\]

\sk
\item Hubbard-Stratonovich変換の公式は？
\[
\exp\left(\alpha x^2\right) = \int du \exp\left(-\alpha u^2 +2\alpha x u\right)
\]
\sk
\item $[Z^n]$の求め方は？
\begin{align}
    [Z^n]
    =&\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\int \prod_i d\tau_i\,P(\{\tau_i\}|\{\xi_i\})P(\{\xi_i\}) \left(\mathop{\mathrm{Tr}}\limits_{\{\sigma_i\}}\exp(-\beta H)\right)^n \\
    =&\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\int \prod_i d\tau_i\,\exp\left(-\sum_i\frac{(\tau_i-\tau_0\xi_i)^2}{2\tau^2}\right)\exp\left(\frac{\beta_s}{2N}\sum_{i\neq j}\xi_i\xi_j\right) \mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}}\exp\left(\frac{\beta_m}{2 N}\displaystyle\sum_\alpha\sum_{i\neq j}\sigma_i^\alpha \sigma_j^\alpha + \beta\displaystyle\sum_\alpha\sum_i \tau_i\sigma_i^\alpha\right)\\
    =&\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\int \prod_i d\tau_i\,\exp\left(-\sum_i\frac{\tau_i^2+\tau_0^2}{2\tau^2}+\frac{\tau_0}{\tau^2}\sum_i\tau_i\xi_i\right)\exp\left[\frac{\beta_s}{2N}\left(\sum_i \xi_i\right)^2-\frac{\beta_s}{2}\right]\\
    &\times\mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}}\exp\left[\frac{\beta_m}{2 N}\displaystyle\sum_\alpha\left(\sum_{i}\sigma_i^\alpha\right)^2 + \beta\displaystyle\sum_\alpha\sum_i \tau_i\sigma_i^\alpha-n\frac{\beta_m}{2}\right]\\
    =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2})\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}} \int \prod_i d\tau_i\,\prod_i\exp\left[-\frac{1}{2\tau^2}\tau_i^2+\left(\frac{\tau_0}{\tau^2}\xi_i+\beta\displaystyle\sum_\alpha\sigma_i^\alpha\right)\tau_i\right]\\
    &\times \int dm_0 \,\exp\left(-\frac{N\beta_s}{2}m_0^2+\beta_s m_0\sum_i\xi_i \right)\int \prod_\alpha dm_\alpha \prod_\alpha \exp\left(-\frac{N\beta_m}{2}m_\alpha^2 + \beta_m m_\alpha\sum_i\sigma_i^\alpha\right)\\
    =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2})\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}} \prod_i\exp\left[\frac{\tau^2}{2}\left(\frac{\tau_0}{\tau^2}\xi_i+\beta\displaystyle\sum_\alpha\sigma_i^\alpha\right)^2\right]\\
    &\times \int dm_0 \,\exp\left(-\frac{N\beta_s}{2}m_0^2+\beta_s m_0\sum_i\xi_i \right)\int \prod_\alpha dm_\alpha \prod_\alpha\exp\left(-\frac{N\beta_m}{2}m_\alpha^2 + \beta_m m_\alpha\sum_i\sigma_i^\alpha\right)\\
    =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2} +\frac{\tau_0^2}{2\tau^2})\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}} \prod_i\exp\left( \beta\tau_0 \xi_i \displaystyle\sum_\alpha\sigma_i^\alpha\right)\int du \,\exp\left(-\frac{\beta^2 \tau^2}{2}u+\beta^2\tau^2 \displaystyle\sum_\alpha\sigma_i^\alpha \, u\right)\\
    &\times \int dm_0 \,\exp\left(-\frac{N\beta_s}{2}m_0^2+\beta_s m_0\sum_i\xi_i \right)\int \prod_\alpha dm_\alpha\prod_\alpha \exp\left(-\frac{N\beta_m}{2}m_\alpha^2 + \beta_m m_\alpha\sum_i\sigma_i^\alpha\right)\\
     =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2} +\frac{\tau_0^2}{2\tau^2})\mathop{\mathrm{Tr}}\limits_{\{\xi_i\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma_i^\alpha\}} \prod_i\exp\left( \beta\tau_0 \xi_i \displaystyle\sum_\alpha\sigma_i^\alpha\right)\int Du \exp\left(\beta\tau \displaystyle\sum_\alpha\sigma_i^\alpha \, u\right)\\
    &\times \int dm_0 \,\prod_i\exp\left(-\frac{\beta_s}{2}m_0^2+\beta_s m_0\xi_i \right)\int \prod_\alpha dm_\alpha \prod_\alpha \prod_i \exp\left(-\frac{\beta_m}{2}m_\alpha^2 + \beta_m m_\alpha\sigma_i^\alpha\right)\\
    =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2} +\frac{\tau_0^2}{2\tau^2})\int dm_0 \int \prod_\alpha dm_\alpha \exp\left(-\frac{N\beta_s}{2}m_0^2 -\frac{N\beta_m}{2}m_\alpha^2 \right)\\
     &\times \,\bigg[\mathop{\mathrm{Tr}}\limits_{\{\xi\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma^\alpha\}} \int Du \exp\left(\beta_s m_0\xi +\beta_m \sum_\alpha m_\alpha\sigma^\alpha+\beta\tau_0 \xi \displaystyle\sum_\alpha\sigma^\alpha +\beta\tau \displaystyle\sum_\alpha\sigma^\alpha \, u \right)\bigg]^N\\
         =&\exp(-\frac{N\tau_0^2}{2\tau^2}-\frac{\beta_s}{2}-\frac{n\beta_m}{2} +\frac{\tau_0^2}{2\tau^2})\int dm_0 \int \prod_\alpha dm_\alpha \exp N\bigg(-\frac{\beta_s}{2}m_0^2 -\frac{\beta_m}{2}m_\alpha^2 \\ 
     &+\ln \left[\mathop{\mathrm{Tr}}\limits_{\{\xi\}}\mathop{\mathrm{Tr}}\limits_{\{\sigma^\alpha\}} \int Du \exp\left(\beta_s m_0\xi +\beta_m \sum_\alpha m_\alpha\sigma^\alpha+\beta\tau_0 \xi \displaystyle\sum_\alpha\sigma^\alpha +\beta\tau \displaystyle\sum_\alpha\sigma^\alpha \, u \right)\right]\bigg)\\
\end{align}
途中で、Hubbard-Stratonovich、 Gauss積分を用いた。
一般に、$\Tr$と$\int$は可換である。


\sk

\item RS解の自己無撞着方程式は？
\begin{align}
    m_0 &= \tanh \beta_s m_0\\\\
m&=\frac{\mathop{\mathrm{Tr}}\limits_{\xi} \int Du\, e^{\beta_s m_0 \xi} \,\tanh \left(\beta_m m + \beta\tau_0\xi + \beta\tau u  \right)}{2\cosh\beta_s m_0}\\\\
M &= \frac{\mathop{\mathrm{Tr}}\limits_{\xi} \int Du \,e^{\beta_s m_0 \xi} \, \mathrm{sgn} \left(\beta_m m + \beta\tau_0\xi + \beta\tau u  \right)}{2\cosh\beta_s m_0}
\end{align}
\end{itemize}

\subsection{重なりの温度依存性}




\end{document}